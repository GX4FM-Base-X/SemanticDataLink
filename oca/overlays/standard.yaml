attribute:
  - name: standard
    properties:
      title: 'standard'
      slot_uri: sdl:standard
      range: string
      description: 'The technical or domain-specific standard the dataset conforms to.'
      multivalued: false
      required: false
      pattern: '^[A-Za-z0-9\-\_]+$'
