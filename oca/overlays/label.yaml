attribute:
  - name: label
    properties:
      title: 'label'
      slot_uri: sdl:label
      range: string
      description: 'A human-readable identifier for the dataset or its elements.'
      multivalued: false
      required: true
      pattern: '^.+$'
